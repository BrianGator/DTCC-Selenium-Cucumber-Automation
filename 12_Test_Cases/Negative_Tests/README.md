# Negative Tests

Purpose: Verifies invalid inputs are rejected with clear validation behavior.

Executable implementation: `src/test/java/com/dtcc/automation/negative/NegativeLoginValidationTest.java`

Run command example:

```bash
mvn test -Dtest=NegativeLoginValidationTest test
```

Reviewer note: this folder is a navigation-friendly copy/reference area. The executable Maven source lives under `src/test/java` so the project follows standard Java automation structure.
