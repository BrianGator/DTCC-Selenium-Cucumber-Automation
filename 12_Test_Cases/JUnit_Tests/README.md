# JUnit Tests

This folder documents and demonstrates the junit tests layer of the automation project.

## What it validates

- Test intent and scope for this category.
- Example implementation or executable source reference.
- How the folder maps to the optimized Java/Maven project under `src/test/java` or `src/test/resources`.

## Main source location

See the code comments in `OrderCalculatorJUnitExample.java` for the exact implementation path and Maven command.
