# Visual Smoke Tests

Purpose: Captures a screenshot artifact for visual review.

Executable implementation: `src/test/java/com/dtcc/automation/visual/PageTemplateSnapshotTest.java`

Run command example:

```bash
mvn test -Dtest=PageTemplateSnapshotTest test
```

Reviewer note: this folder is a navigation-friendly copy/reference area. The executable Maven source lives under `src/test/java` so the project follows standard Java automation structure.
