# PDF Download Tests

Purpose: Validates configured PDF download links and content type.

Executable implementation: `src/test/java/com/dtcc/automation/pdf/PdfDownloadValidationTest.java`

Run command example:

```bash
mvn test -Dtest=PdfDownloadValidationTest test
```

Reviewer note: this folder is a navigation-friendly copy/reference area. The executable Maven source lives under `src/test/java` so the project follows standard Java automation structure.
