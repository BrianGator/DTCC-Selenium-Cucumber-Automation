# Smoke Tests

Purpose: Fast public-site smoke test verifying page title and body content.

Executable implementation: `src/test/java/com/dtcc/automation/smoke/PublicSiteSmokeTest.java`

Run command example:

```bash
mvn test -Dtest=PublicSiteSmokeTest test
```

Reviewer note: this folder is a navigation-friendly copy/reference area. The executable Maven source lives under `src/test/java` so the project follows standard Java automation structure.
